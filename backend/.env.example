# Copie este arquivo para .env e ajuste os valores
# Em produção (Railway), configure estas variáveis no painel de Environment Variables

DATABASE_URL=postgresql://postgres:senha@localhost:5432/gf_cobrar
SECRET_KEY=troque-por-uma-chave-secreta-longa-e-aleatoria
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Em produção, coloque a URL do seu frontend Vercel:
# CORS_ORIGINS=["https://gf-cobrar.vercel.app"]
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
